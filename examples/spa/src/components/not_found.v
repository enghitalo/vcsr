// 404 fallback route — LOGIC only (template: not_found.html, styles:
// not_found.css). No state. Illustrative source.
module components

import vcsr

@[component]
pub struct NotFound {
	vcsr.Component
}
